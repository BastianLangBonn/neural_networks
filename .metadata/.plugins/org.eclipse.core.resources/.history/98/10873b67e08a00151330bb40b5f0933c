import java.util.Arrays;
import java.util.List;

import model.MLP;

/**
 * 
 * @author bastian
 * 
 */
public class Main {
    public static void main(String[] args) {
        MLP mlp = new MLP(2, 2, 1, 0.5);
        System.out.println(mlp);
        List<Double> input = Arrays.asList(1, 1);
        mlp.propagate(input);
    }
}
