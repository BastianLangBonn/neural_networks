import java.util.ArrayList;
import java.util.Arrays;

import model.MLP;

/**
 * 
 * @author bastian
 * 
 */
public class Main {
    public static void main(String[] args) {
        MLP mlp = new MLP(2, 2, 1, 0.5);
        System.out.println(mlp);
        mlp.propagate(new ArrayList<Double>(Arrays.asList(1, 1)));
    }
}
